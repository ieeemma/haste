
# Simple factorial example program
{
    fn factorial(n) {
        let total = 0;
        while (n > 0) {
            total *= n;
            n -= 1;
        };
        n;
    };
    factorial(5);
}

